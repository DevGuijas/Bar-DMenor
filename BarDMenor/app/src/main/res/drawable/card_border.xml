<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Preencha o fundo de branco -->
    <stroke
        android:width="2dp"
        android:color="@color/black"></stroke> <!-- Linha preta ao redor -->
    <corners android:radius="12dp"/> <!-- Cantos arredondados -->
</shape>
